import{bV as a,aL as s,u as r,G as o}from"./chunks/framework.DOOe5dLR.js";const H=JSON.parse('{"title":"HBase","description":"","frontmatter":{},"headers":[],"relativePath":"java/大数据/hBase.md","filePath":"java/大数据/hBase.md","lastUpdated":1684392117000}'),t={name:"java/大数据/hBase.md"};function i(n,e,l,p,g,h){return s(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="hbase" tabindex="-1">HBase <a class="header-anchor" href="#hbase" aria-label="Permalink to &quot;HBase&quot;">​</a></h1><p>HBase 是一个面向列式存储的分布式数据库。HBase 底层存储基于 HDFS 实现，集群的管理基于 ZooKeeper 实现。HBase 良好的分布式架构设计为海量数据的快速存储、随机访问提供了可能，基于数据副本机制和分区机制可以轻松实现在线扩容、缩容和数据容灾，是大数据领域中 Key-Value 数据结构存储最常用的数据库方案。HBase 集群主由HMaster、Region Server 和 ZooKeeper 组成。</p><p>HBase 的核心架构由五部分组成，分别是 HBase Client、HMaster、HRegionServer、ZooKeeper 以及 HDFS。</p><p>HMaster 是 HBase 集群的主节点，负责整个集群的管理工作，主要工作职责如下：</p><ul><li>分配Region：负责启动的时候分配Region到具体的 RegionServer；</li><li>负载均衡：一方面负责将用户的数据均衡地分布在各个 Region Server 上，防止Region Server数据倾斜过载。另一方面负责将用户的请求均衡地分布在各个 Region Server 上，防止Region Server 请求过热；</li><li>维护数据：发现失效的 Region，并将失效的 Region 分配到正常的 RegionServer 上，并且在Region Sever 失效的时候，协调对应的HLog进行任务的拆分。</li></ul><p>HregionServer 直接对接用户的读写请求，是真正的“干活”的节点。的功能概括如下：</p><ol><li>管理 master 为其分配的Region</li><li>处理来自客户端的读写请求</li><li>负责和底层 HDFS的交互，存储数据到 HDFS</li><li>负责 Region变大以后的拆分</li><li>负责Storefile 的合并工作</li></ol><h2 id="hbase相关概念" tabindex="-1">HBase相关概念 <a class="header-anchor" href="#hbase相关概念" aria-label="Permalink to &quot;HBase相关概念&quot;">​</a></h2><p>**表：**HBase的数据同样是用表来组织的，表由行和列组成，列分为若干个列族，行和列的坐标交叉决定了一个单元格。</p><p>**行：**每个表由若干行组成，每个行有一个行键作为这一行的唯一标识。访问表中的行只有三种方式：通过单个行键进行查询、通过一个行键的区间来访问、全表扫描。</p><p>**列族：**一个HBase表被分组成许多“列族”的集合，它是基本的访问控制单元。</p><p>**列修饰符（列限定符）：**列族里的数据通过列限定符（或列）来定位</p><p>**单元格：**在HBase表中，通过行、列族和列限定符确定一个“单元格”（cell），单元格中存储的数据没有数据类型，总被视为字节数组byte[]</p><h3 id="hbase数据模型" tabindex="-1">HBase数据模型 <a class="header-anchor" href="#hbase数据模型" aria-label="Permalink to &quot;HBase数据模型&quot;">​</a></h3><p>HBase将数据存放在带有标签的<strong>表</strong>中，表由<strong>行和列</strong>组成，行和列交叉确定一个<strong>单元格</strong>，单元格有<strong>版本号</strong>，版本号自动分配，为数据插入该单元格时的时间戳。单元格的内容没有数据类型，<strong>所有数据都被视为未解释的字节数组</strong>。</p><p>表格中每一行有一个<strong>行键</strong>（也是字节数组，任何形式的数据都可以表示成字符串，比如数据结构进行序列化之后），<strong>整个表根据行键的字节序来排序</strong>，所有对表的访问必须通过行键。Hbase 只支持 3 中查询方式：基于 Rowkey 的单行查询，基于 Rowkey 的范围扫描，全表扫描。</p><p>表中的列又划分为多个<strong>列族</strong>（column family），同一个列族的所有成员具有相同的前缀，具体的列由列修饰符标识，因此，<strong>列族和列修饰符</strong>合起来才可以表示某一列</p><p>HBase中需要根据行键、列族、列限定符和时间戳来确定一个单元格，因此，可以视为一个“<strong>四维坐标</strong>”，即**[行键, 列族, 列限定符, 时间戳]**。</p><p><img src="https://img2018.cnblogs.com/blog/1608161/201906/1608161-20190625141609565-1141190098.png" alt="img"></p><h3 id="region" tabindex="-1">Region <a class="header-anchor" href="#region" aria-label="Permalink to &quot;Region&quot;">​</a></h3><p>HBase自动把表水平划分为<strong>区域</strong>（Region），每个区域都是有若干连续行构成的，一个区域由**所属的表、起始行、终止行（不包括这行）**三个要素来表示。Region 的概念和关系型数据库的分区或者分片差不多。</p><p>一开始，一个表只有一个区域，但是随着数据的增加，区域逐渐变大，等到它超出设定的阈值大小，就会在某行的边界上进行拆分，分成两个大小基本相同的区域。然后随着数据的再增加，区域就不断的增加，如果超出了单台服务器的容量，就可以把一些区域放到其他节点上去，构成一个集群。也就是说：<strong>集群中的每个节点（Region Server）管理整个表的若干个区域</strong>。所以，我们说：<strong>区域是HBase集群上分布数据的最小单位</strong>。</p><h3 id="timestamp" tabindex="-1">TimeStamp <a class="header-anchor" href="#timestamp" aria-label="Permalink to &quot;TimeStamp&quot;">​</a></h3><p>TimeStamp是实现Hbase 多版本的关键。在Hbase 中使用不同的timestame 来标识相同rowkey行对应的不通版本的数据。在写入数据的时候，如果用户没有指定对应的timestamp，Hbase 会自动添加一个 timestamp，timestamp和服务器时间保持一致。在Hbase中，相同rowkey的数据按照timestamp倒序排列。默认查询的是最新的版本，用户可同指定timestamp的值来读取旧版本的数据。</p><h2 id="hdfs" tabindex="-1">HDFS <a class="header-anchor" href="#hdfs" aria-label="Permalink to &quot;HDFS&quot;">​</a></h2><p>HDFS 为Hbase提供最终的底层数据存储服务，同时为 Hbase提供高可用（Hlog存储在HDFS）的支持。</p>',26)])])}const d=a(t,[["render",i]]);export{H as __pageData,d as default};
